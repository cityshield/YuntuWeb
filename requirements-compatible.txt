# 兼容性更好的Python依赖列表
# 适用于 CentOS 7.9 和较低版本的Python

# Web 框架 - 兼容Python 3.6+
Flask==2.0.3
flask-cors==3.0.10

# HTTP 请求库 - 兼容版本
requests==2.25.1

# 图像处理 - 兼容版本
Pillow==8.4.0
# EXR 支持需要 OpenEXR
OpenEXR==1.3.9

# 其他标准库依赖（Python 内置，无需安装）
# - json
# - os
# - base64
# - io

# 可选：用于生产环境部署
# gunicorn==20.1.0
# gevent==21.12.0
